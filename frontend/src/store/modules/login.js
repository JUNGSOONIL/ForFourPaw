const state = {
  isLogin: false,
};

// getters
const getters = {
  isLogin: function (state) {
    return state.isLogin;
  },
};

// mutations
const mutations = {};

// actions
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
